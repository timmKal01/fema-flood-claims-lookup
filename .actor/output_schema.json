{
    "actorOutputSchemaVersion": 1,
    "title": "Output schema of FEMA Flood Claims Lookup",
    "description": "One record per NFIP flood insurance claim matching the search, with location, flood event, and payout details.",
    "properties": {
        "results": {
            "type": "string",
            "title": "Results",
            "template": "{{links.apiDefaultDatasetUrl}}/items"
        }
    }
}
